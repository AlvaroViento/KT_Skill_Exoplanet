# 使用指南

## 1. 选择输入模式

- `text_concept`：文字构思、提纲或口述转写，默认目标为 `develop`。
- `board_draft`：零散草图、便签或未形成统一版式的画板，默认目标为 `revise`。
- `final_sheet`：已有完整标题、分区、流程或界面的整张快题，默认目标为 `retrospective`。

需要提取优秀方法时，可明确指定 `review_goal=benchmark`。

```text
使用 $quick-sketch-logic-review 完整复盘这张快题成图。
```

## 2. 可以提交什么

- 一句话概念或完整文字方案。
- 草图、画板截图或局部方案图。
- 完整快题成图。
- 考试方向、题目要求、院校语境或剩余修改时间等必要约束。

材料不完整也可以使用。图中文字看不清时，Skill 应标记为 `unreadable`，而不是断言作者没有考虑。

## 3. 输出内容

审查按“问题 → 对象 → 策略 → 实现 → 效果”还原方案，并依次给出总体判断、默认三条最高优先级问题、模块 Review、必要的外部事实核验、纸面证据、实现硬伤和下一步最小修改。

`final_sheet` 或 `benchmark` 默认包含可复用优点。每个 `fatal` 或 `major` 问题只给一个首选最小修复；需要更换核心对象、策略或实现时会明确标记为“方案级调整”。

## 4. 连续修改

```text
我按上一轮修改成这样，请继续用 $quick-sketch-logic-review 检查。
```

优先处理 `fatal`，其次处理跨模块或实现硬伤类 `major`，再处理纸面缺口。

## 5. 使用边界

- Skill 是训练反馈工具，不替代教师、真实用户研究或院校专项判断。
- 外部资料只能核验现实，不能算作图面已经呈现的内容。
- 不要求所有专业补齐相同模块，只检查当前方案真正涉及的角色、技术、材料、服务或空间条件。
- 不因缺少完整预算、商业模型或技术架构而直接判定方案失败。
